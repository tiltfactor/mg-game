<?php

/**
 * This is the model base class for the table "user_online".
 * DO NOT MODIFY THIS FILE! It is automatically generated by giix.
 * If any changes are necessary, you must set or override the required
 * property or method in class "UserOnline".
 *
 * Columns in table "user_online" available as properties of the model,
 * followed by relations of table "user_online" available as properties of the model.
 *
 * @property integer $id
 * @property integer $user_id
 * @property integer $session_id
 * @property integer $game_id
 * @property integer $played_game_id
 * @property integer $status
 * @property string $created
 *
 * @property User $user
 * @property Session $session
 * @property Game $game
 * @property PlayedGame $playedGame
 */
abstract class BaseUserOnline extends GxActiveRecord {

	public static function model($className=__CLASS__) {
		return parent::model($className);
	}

	public function tableName() {
		return 'user_online';
	}

	public static function label($n = 1) {
		return Yii::t('app', 'UserOnline|UserOnlines', $n);
	}

	public static function representingColumn() {
		return 'created';
	}

	public function rules() {
		return array(
			array('user_id, session_id, game_id, created', 'required'),
			array('user_id, session_id, game_id, played_game_id, status', 'numerical', 'integerOnly'=>true),
			array('played_game_id, status', 'default', 'setOnEmpty' => true, 'value' => null),
			array('id, user_id, session_id, game_id, played_game_id, status, created', 'safe', 'on'=>'search'),
		);
	}

	public function relations() {
		return array(
			'user' => array(self::BELONGS_TO, 'User', 'user_id'),
			'session' => array(self::BELONGS_TO, 'Session', 'session_id'),
			'game' => array(self::BELONGS_TO, 'Game', 'game_id'),
			'playedGame' => array(self::BELONGS_TO, 'PlayedGame', 'played_game_id'),
		);
	}

	public function pivotModels() {
		return array(
		);
	}

	public function attributeLabels() {
		return array(
			'id' => Yii::t('app', 'ID'),
			'user_id' => null,
			'session_id' => null,
			'game_id' => null,
			'played_game_id' => null,
			'status' => Yii::t('app', 'Status'),
			'created' => Yii::t('app', 'Created'),
			'user' => null,
			'session' => null,
			'game' => null,
			'playedGame' => null,
		);
	}

	public function search() {
		$criteria = new CDbCriteria;

		$criteria->compare('id', $this->id);
		$criteria->compare('user_id', $this->user_id);
		$criteria->compare('session_id', $this->session_id);
		$criteria->compare('game_id', $this->game_id);
		$criteria->compare('played_game_id', $this->played_game_id);
		$criteria->compare('status', $this->status);
		$criteria->compare('created', $this->created, true);

		return new CActiveDataProvider($this, array(
			'criteria' => $criteria,
			'pagination'=>array(
        'pageSize'=>Yii::app()->fbvStorage->get("settings.pagination_size"),
      ),
		));
	}
}